#!/bin/bash

## Подготовка bash-скрипта к релизу.
## удаляет из кода bash все коментарии
## и производит компактное форматирование кода

env "BASH_FUNC_tmp_%%=() {
$(cat "${1}")
}" bash -c 'declare -f tmp_' | tail -n+2 | sed  '1d;$d'
